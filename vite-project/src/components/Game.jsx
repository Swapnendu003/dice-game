import React from 'react';
import styled from 'styled-components';
import Score from '../components/Score'

const Game = ({ toggle }) => {
  return (
    <main>
    <Score />
    </main>
  );
}

export default Game;
